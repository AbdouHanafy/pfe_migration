apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: migration-data-pvc
  namespace: migration-pfe
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: standard
